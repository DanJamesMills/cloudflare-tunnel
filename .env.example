# Give your tunnel a friendly name to identify it in the dashboard
TUNNEL_NAME=My Tunnel

TUNNEL_TOKEN=

# Dashboard credentials (optional - only needed if using the dashboard)
DASHBOARD_USER=admin
DASHBOARD_PASSWORD=admin
DASHBOARD_SECRET_KEY=change-this-to-a-random-string

# Maximum number of log entries to keep in memory and file (default: 500)
MAX_LOG_ENTRIES=500

# Path to the log file (default: /app/logs/access.log in container, maps to ./logs/access.log on host)
LOG_FILE_PATH=/app/logs/access.log
